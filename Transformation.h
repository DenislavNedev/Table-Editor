#ifndef TRANSFORMATION_H
#define TRANSFORMATION_H
#include "String.h"
#include <cmath>

class Transformation
{
public:
	int transform_into_int(String value);
	double transform_into_fraction(String value);
	
};
#endif // !TRANSFORMATION_H
